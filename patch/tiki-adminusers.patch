--- tiki-adminusers.php.orig	2010-03-07 23:54:41.000000000 -0300
+++ tiki-adminusers.php	2010-06-07 18:35:06.000000000 -0300
@@ -504,6 +504,10 @@
 						'num' => 0,
 						'mes' => sprintf(tra("%s changed from %s to %s") , tra("login") , $userinfo['login'], $_POST["name"])
 					);
+					if (!isset($elgallib)) {
+						require_once("lib/elgal/elgallib.php");
+					}
+					$elgallib->change_login_acervo($userinfo['login'],$_POST['name']);
 					$logslib->add_log('adminusers', 'changed login for ' . $_POST['name'] . ' from ' . $userinfo['login'] . ' to ' . $_POST["name"]);
 					$userinfo['login'] = $_POST['name'];
 					if ($prefs['login_is_email'] == 'y') {
